    <%- include('header.ejs') %>
    <main class="d-flex justify-content-center container-fluid flex-fill">
        <section>
            <h1>Dashboard</h1>
            <table class="table table-striped">
                <thead>
                    <tr class="table-primary">
                        <th colspan="3"></th>
                    </tr>
                </thead>
                <tbody>
                    <tr class="table-dark">
                        <th>Debts</th>
                        <th>Current</th>
                        <th>Late</th>
                    </tr>
                    <% if (typeof debtors !== 'undefined'){ %>
                    <tr>
                        <td><%= debtors.debtsCount %></td>
                        <td><%= debtors.currentPayments %></td>
                        <td><%= debtors.latePayments %></td>
                    </tr>
                    <% } %>
                </tbody>
            </table>
        </section>  
    </main>
    
<%- include('footer.ejs') %>